# platform-portal frontend

React/Vite web UI for the Platform Portal.

## Local Run

```bash
npm install
npm run dev
```

Open:

```text
http://localhost:3001
```

The frontend expects the API at:

```text
http://localhost:8081
```
